# 多飞书告警实施计划

1. 扩展模型和幂等迁移，建立 `service_alert_configs` 多对多关系并迁移旧单例数据。
2. 替换单例告警 API 为列表 CRUD/测试 API，服务 API 改为 `alert_config_ids`。
3. 监测器只读取服务关联的启用告警配置并聚合发送结果。
4. 重做告警管理页面和服务多选界面，更新类型与 API 客户端。
5. 添加 API 回归测试，运行 Python 编译、前端类型检查和可用测试。

兼容边界：不删除旧表、旧列或 Webhook 密文；旧路径不再作为运行时事实来源。
